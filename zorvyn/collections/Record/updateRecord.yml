info:
  name: updateRecord
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:8080/api/v1/records/{{id}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
