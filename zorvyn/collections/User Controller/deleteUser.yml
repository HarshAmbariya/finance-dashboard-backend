info:
  name: deleteUser
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:8080/api/v1/users/{{id}}
  auth: inherit

runtime:
  variables:
    - name: id
      value: "3"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
