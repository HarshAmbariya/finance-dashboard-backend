info:
  name: getSummary
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/v1/dashboard/summary
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
