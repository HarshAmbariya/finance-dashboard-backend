info:
  name: updateUser
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:8080/api/v1/users/{{id}}
  auth: inherit

runtime:
  variables:
    - name: id
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
