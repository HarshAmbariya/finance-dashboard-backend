info:
  name: getCurrentUser
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8080/api/v1/users/me
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
