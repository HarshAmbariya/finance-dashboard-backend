info:
  name: deleteRecord
  type: http
  seq: 4

http:
  method: DELETE
  url: http://localhost:8080/api/v1/records/{{id}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
