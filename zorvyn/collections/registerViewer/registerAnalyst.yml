info:
  name: registerAnalyst
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/api/v1/auth/register/analyst
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
