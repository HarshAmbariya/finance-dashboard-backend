info:
  name: login
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/api/v1/auth/login
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
