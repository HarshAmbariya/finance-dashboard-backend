info:
  name: registerAdmin
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/v1/auth/register/admin
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
